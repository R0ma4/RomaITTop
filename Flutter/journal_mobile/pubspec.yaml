name: journal_mobile
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  yaml: ^3.1.0
  path_provider: ^2.0.11
  shared_preferences: ^2.2.3
  intl: ^0.18.0
  ini: ^2.1.0
  flutter_local_notifications: ^19.4.2
  connectivity_plus: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - lib/settings/config.conf
